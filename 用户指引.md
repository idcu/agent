# IDCU Agent 用户指引

欢迎使用 IDCU Agent！这篇文档会用最简单的方式教你怎么用这个程序。

---

## 目录

1. [准备工作](#准备工作)
2. [编译程序](#编译程序)
3. [运行程序](#运行程序)
4. [配置程序](#配置程序)
5. [常见问题](#常见问题)

---

## 准备工作

在开始之前，你需要先安装一些工具。

### Windows 用户

你需要安装 **MinGW**（一个 Windows 上的 C 语言编译器）。

怎么安装 MinGW：
1. 去网上搜索 "MinGW-w64" 下载
2. 或者用更简单的方法：安装 **TDM-GCC**（推荐新手用这个）
3. 安装完成后，打开命令提示符（cmd），输入 `gcc --version`，如果有输出说明安装成功了

### Linux 用户

Linux 系统通常已经自带了编译器。打开终端，输入：
```bash
gcc --version
```
如果有输出说明可以用了。如果没有，用下面的命令安装：

**Ubuntu/Debian:**
```bash
sudo apt-get install gcc make
```

**CentOS/Fedora:**
```bash
sudo yum install gcc make
```

---

## 编译程序

### 方法一：最简单的方法（推荐新手）

#### Windows 用户
直接双击项目文件夹里的 `build.bat` 文件，等它运行完就可以了！

#### Linux 用户
打开终端，进入项目文件夹，运行：
```bash
chmod +x build.sh
./build.sh
```

### 方法二：用 CMake 编译

如果你熟悉 CMake，也可以用它：

#### Windows 用户
```bash
mkdir build
cd build
cmake -G "MinGW Makefiles" ..
mingw32-make
```

#### Linux 用户
```bash
mkdir build
cd build
cmake ..
make
```

---

## 运行程序

编译成功后，程序会在 `out` 文件夹里。

### Windows 用户
双击 `out/idcu_agent.exe`，或者在命令提示符里运行：
```bash
cd out
idcu_agent.exe
```

### Linux 用户
```bash
cd out
./idcu_agent
```

### 停止程序
想停止程序时，按 `Ctrl + C` 就可以了。

---

## 配置程序

程序的配置文件在 `config/agent.cfg`。

你可以用记事本（Windows）或文本编辑器（Linux）打开它来修改配置。

### 配置项说明

配置文件使用 INI 格式，包含以下主要配置项：

```ini
[general]
version = 1.0.0
log_level = info
log_file = agent.log
max_modules = 32
max_messages = 128

[modules]
enable_base_log = true
enable_core_module = true
enable_biz_collect = true

[permissions]
base_log = log,run
core_module = module_mgr,config,run
biz_collect = send,recv,run,hw

[resources]
sandbox_quota = 1048576
coroutine_priority = normal
msg_queue_size = 128
```

**提示**：修改配置后，需要重启程序才能生效。

---

## 常见问题

### Q: 双击 build.bat 没反应？

A: 可能是没有安装 MinGW。先按照"准备工作"里的说明安装编译器。

### Q: 编译时提示找不到命令？

A: Windows 用户需要把 MinGW 的 bin 目录加到系统环境变量 PATH 里。或者用 "MinGW Terminal" 来运行命令。

### Q: 运行程序时一闪就没了？

A: 不要直接双击 exe 文件，先打开命令提示符，进入 out 文件夹，然后输入 `idcu_agent.exe` 来运行，这样就能看到错误信息了。

### Q: 怎么清理编译出来的文件？

A: 直接删除 `out` 和 `build` 文件夹。

### Q: 我可以把程序放到其他电脑上运行吗？

A: 可以！但是要注意：
- Windows 上编译的只能在 Windows 上跑
- Linux 上编译的只能在 Linux 上跑

---

## 下一步

如果你想：
- 了解这个程序是怎么工作的
- 修改代码
- 添加自己的功能

请看 [开发者指引.md](./开发者指引.md)

---

有问题？别担心，多试几次就会了！加油！💪
