# 配置集成配置
config:
  # 配置文件默认路径
  path: "config/default"
  
  # 是否启用热重载
  hot_reload: true

  # 默认配置格式: yaml, json
  format: "yaml"

  # 多环境配置
  environment:
    current: "development"
    available:
      - "development"
      - "testing"
      - "production"
