#include "idcu/os/os.h"

int idcu_sysinfo_get(idcu_sysinfo_t* info) { (void)info; return -1; }
int idcu_statvfs_get(const char* path, idcu_statvfs_t* statvfs_out) { (void)path; (void)statvfs_out; return -1; }
