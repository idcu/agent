name: idcu-module-verifier
version: 1.0.0
description: Module-Verifier library for IDCU Agent
author: IDCU Team
license: Apache-2.0

dependencies:
  - idcu-common

platform_link_libraries:
  unix:
    - pthread

build:
  type: cmake
  targets:
    - idcu-module-verifier

testing:
  enabled: true
