# idcu-discovery

Discovery library for IDCU Agent.

## ???????
- ???????????????
- ??????????????- ?????????
- [???????????

## ??????

`c
#include <idcu/discovery/discovery.h>

idcu_Discovery_Context* ctx;
int ret = idcu_discovery_init(&ctx);
if (ret == IDCU_ERR_OK) {
    // ?????    idcu_discovery_destroy(ctx);
}
`

## API ????
- idcu_discovery_init() - ?????- idcu_discovery_destroy() - ????- idcu_discovery_is_initialized() - ????????????- idcu_discovery_get_operation_count() - ?????????
- idcu_discovery_get_error_count() - ?????????

## ???

`ash
cmake -B build && cmake --build build
`

## ?????
MIT
