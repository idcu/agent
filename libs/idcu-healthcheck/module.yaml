name: idcu-healthcheck
version: 1.0.0
description: Healthcheck library for IDCU Agent
author: IDCU Team
license: Apache-2.0

dependencies:
  - idcu-common

build:
  type: cmake
  targets:
    - idcu-healthcheck

testing:
  enabled: true
  framework: internal
