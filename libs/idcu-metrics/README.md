# idcu-metrics

Metrics library for IDCU Agent.

## ???????
- ???????????????
- ??????????????- ?????????
- [???????????

## ??????

`c
#include <idcu/metrics/metrics.h>

idcu_Metrics_Context* ctx;
int ret = idcu_metrics_init(&ctx);
if (ret == IDCU_ERR_OK) {
    // ?????    idcu_metrics_destroy(ctx);
}
`

## API ????
- idcu_metrics_init() - ?????- idcu_metrics_destroy() - ????- idcu_metrics_is_initialized() - ????????????- idcu_metrics_get_operation_count() - ?????????
- idcu_metrics_get_error_count() - ?????????

## ???

`ash
cmake -B build && cmake --build build
`

## ?????
MIT
