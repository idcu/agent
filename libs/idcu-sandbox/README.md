# idcu-sandbox

Sandbox library for IDCU Agent.

## ???????
- ???????????????
- ??????????????- ?????????
- [???????????

## ??????

`c
#include <idcu/sandbox/sandbox.h>

idcu_Sandbox_Context* ctx;
int ret = idcu_sandbox_init(&ctx);
if (ret == IDCU_ERR_OK) {
    // ?????    idcu_sandbox_destroy(ctx);
}
`

## API ????
- idcu_sandbox_init() - ?????- idcu_sandbox_destroy() - ????- idcu_sandbox_is_initialized() - ????????????- idcu_sandbox_get_operation_count() - ?????????
- idcu_sandbox_get_error_count() - ?????????

## ???

`ash
cmake -B build && cmake --build build
`

## ?????
MIT
