name: idcu-http-server
version: 1.0.0
description: HTTP Server library for IDCU Agent
author: IDCU Team
license: Apache-2.0

dependencies:
  - idcu-common
  - idcu-network

build:
  type: cmake
  targets:
    - idcu-http-server

platform_link_libraries:
  unix:
    - pthread

testing:
  enabled: true
  framework: internal
