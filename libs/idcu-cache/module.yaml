name: idcu-cache
version: 1.0.0
description: Cache library for IDCU Agent
author: IDCU Team
license: Apache-2.0

dependencies:
  - idcu-common

build:
  type: cmake
  targets:
    - idcu-cache

testing:
  enabled: true
  framework: internal
