# idcu-watchdog

Watchdog library for IDCU Agent.

## ???????
- ???????????????
- ??????????????- ?????????
- [???????????

## ??????

`c
#include <idcu/watchdog/watchdog.h>

idcu_Watchdog_Context* ctx;
int ret = idcu_watchdog_init(&ctx);
if (ret == IDCU_ERR_OK) {
    // ?????    idcu_watchdog_destroy(ctx);
}
`

## API ????
- idcu_watchdog_init() - ?????- idcu_watchdog_destroy() - ????- idcu_watchdog_is_initialized() - ????????????- idcu_watchdog_get_operation_count() - ?????????
- idcu_watchdog_get_error_count() - ?????????

## ???

`ash
cmake -B build && cmake --build build
`

## ?????
MIT
