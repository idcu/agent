# idcu-conn-pool

Conn-Pool library for IDCU Agent.

## ???????
- ???????????????
- ??????????????- ?????????
- [???????????

## ??????

`c
#include <idcu/conn-pool/conn-pool.h>

idcu_Conn-Pool_Context* ctx;
int ret = idcu_conn-pool_init(&ctx);
if (ret == IDCU_ERR_OK) {
    // ?????    idcu_conn-pool_destroy(ctx);
}
`

## API ????
- idcu_conn-pool_init() - ?????- idcu_conn-pool_destroy() - ????- idcu_conn-pool_is_initialized() - ????????????- idcu_conn-pool_get_operation_count() - ?????????
- idcu_conn-pool_get_error_count() - ?????????

## ???

`ash
cmake -B build && cmake --build build
`

## ?????
MIT
