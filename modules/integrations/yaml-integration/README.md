# yaml-integration

YAML 解析和序列化集成模块。

## 配置

使用 YAML 配置（默认）：

```yaml
yaml:
  output_format: "block"
  indent: 2
  schema_validation: true
```

## 功能

- YAML 解析和序列化
- YAML 与 JSON 互转
- YAML Schema 验证
- YAML 合并和补丁
- 与配置系统、存储系统集成

## 许可证

本库采用 [Apache License 2.0](../../../LICENSE) 许可证。
