# config-integration

配置系统集成模块。

## 配置

使用 YAML 配置（默认）：

```yaml
config:
  path: "config/default"
  hot_reload: true
  format: "yaml"
```

## 功能

- 统一配置管理
- 多格式配置支持（YAML/JSON）
- 配置热重载
- 多环境配置

## 许可证

本库采用 [Apache License 2.0](../../../LICENSE) 许可证。
